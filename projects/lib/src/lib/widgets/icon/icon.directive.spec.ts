import { IconSizeDirective } from './icon.directive';

describe('IconDirective', () => {
  it('should create an instance', () => {
    const directive = new IconSizeDirective();
    expect(directive).toBeTruthy();
  });
});
