import { FormSpaceEditorFormatWrapper, SpacesEditorFormat } from "@codeffekt/ce-core-data";

export interface SpaceEditorInfo {
    form: FormSpaceEditorFormatWrapper;
    project: SpacesEditorFormat;
}